<Window x:Class="CSH_Monitor.UI.Features.EntryWindow.EntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:edge="clr-namespace:CSH_Monitor.UI"
        xmlns:behaviors="clr-namespace:CSH_Monitor.UI.Behaviors"
        xmlns:local="clr-namespace:CSH_Monitor.UI.Features.EntryWindow"
        xmlns:appservice="clr-namespace:CSH_Monitor.UI.AppService"
        mc:Ignorable="d"
        DataContext="{Binding Source={x:Static edge:App.Locator}, Path=EntryWindowViewModel}"
        MinHeight="500" MinWidth="600"
        Title="CSH Monitor V0.0.0-unstable" Height="{Binding Hei}" Width="{Binding Wid}">
    <i:Interaction.Behaviors>
        <behaviors:WindowSizeBehavior Width="{Binding Wid, Mode=OneWayToSource}" Height="{Binding Hei, Mode=OneWayToSource}"/>
    </i:Interaction.Behaviors>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" VerticalAlignment="Top">
            <MenuItem Header="File">

            </MenuItem>
            <MenuItem Header="Edit" >

            </MenuItem>
            <MenuItem Header="View" >

            </MenuItem>
        </Menu>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Здесь будут различные команды" Margin="5,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="Gray">
                <TextBlock Text="возможно здесь будет что либо по проектам" TextWrapping="Wrap"/>
            </Border>
            <Border Grid.Column="1" Background="White">
                <TextBlock Text="здесь будет график и дополнительные показатели" TextWrapping="Wrap" Margin="10,0"/>
            </Border>
            <Border Grid.Column="2" Background="LightGray">
                <TextBlock Text="здесь будет поле ввода данных" TextWrapping="Wrap"/>
            </Border>
        </Grid>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <WrapPanel>
                    <TextBlock Text="Строка состояния (нужно подумать что ещё сюда добавить)" Margin="5,0"/>
                    <TextBlock Text="{Binding Wid}" Margin="5,0"/>
                    <TextBlock Text="{Binding Hei}" Margin="5,0"/>
                </WrapPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>